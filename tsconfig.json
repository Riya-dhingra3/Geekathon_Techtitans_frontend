{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },
    "noUnusedLocals": false, // Set to true to enable strict checks for unused local variables
    "noUnusedParameters": false // Set to true to enable strict checks for unused function parameters
    },
}
